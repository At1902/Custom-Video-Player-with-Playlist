export default function NotFound() {
  return (
    <div
      style={{
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
      }}
    >
      <h1>Oops! It seems like the page you are looking for does not exist</h1>
    </div>
  );
}
